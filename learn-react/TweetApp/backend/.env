PORT= 3003
MONGODB_URI= mongodb://127.0.0.1:27017/mongodb_test
NODE_ENV= development

# Redis — required for topic tree caching (GET /topics). Start Redis locally (e.g. redis-server or Docker).
REDIS_ENABLED=true
# Or use a URL instead of REDIS_ENABLED: REDIS_URL=redis://127.0.0.1:6379