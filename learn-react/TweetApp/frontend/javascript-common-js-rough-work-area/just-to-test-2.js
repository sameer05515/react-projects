const vArray = Array.from({ length: 8 }, (_, i) => `"v3--itr${i + 1}"`);
// const itrArray = Array.from({ length: 20 }, (_, i) => `"itr${i + 1}"`);

console.log("[",vArray.toString(),"]");
// console.log("[",itrArray.toString(),"]");