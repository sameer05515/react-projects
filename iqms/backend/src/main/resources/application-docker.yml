spring:
  datasource:
    url: jdbc:mysql://mysql:3306/${DB_NAME:iqms_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    root: INFO
    com.iqms: INFO
