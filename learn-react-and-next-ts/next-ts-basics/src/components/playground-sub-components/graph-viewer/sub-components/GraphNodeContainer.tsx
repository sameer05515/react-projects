import { GraphNode } from '@/lib/topics'
import React from 'react'

const GraphNodeContainer = ({nodes:[]}:{nodes:GraphNode[]}) => {
  return (
    <div>GraphNodeContainer</div>
  )
}

export default GraphNodeContainer